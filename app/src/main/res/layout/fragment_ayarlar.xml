<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".view.ayarlarFragment">

    <Button
        android:id="@+id/fiyatlarButton"
        android:layout_width="305dp"
        android:layout_height="67dp"
        android:layout_marginTop="250dp"
        android:onClick="fiyatlarButtonClicked"
        android:text="Fiyatlar"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintHorizontal_bias="0.504"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <Button
        android:id="@+id/mesajlarButton"
        android:layout_width="305dp"
        android:layout_height="67dp"
        android:layout_marginTop="8dp"
        android:onClick="mesajlarButtonClicked"
        android:text="Mesajlar"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintHorizontal_bias="0.504"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@+id/fiyatlarButton" />
</androidx.constraintlayout.widget.ConstraintLayout>